{
  "name": "school-erp-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "client", "shared"],
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev:backend": "npm run -w server dev",
    "dev:frontend": "npm run -w client dev",
    "start:backend": "npm run -w server start",
    "build": "npm run -w shared build && npm run -w server build && npm run -w client build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {},
  "optionalDependencies": {}
}
